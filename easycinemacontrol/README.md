# Easy Cinema Control 🎬

Веб-система для централизованного управления залами кинотеатра с проекторами Barco ICMP через протокол Text over IP.

## 🎯 Главный файл

**`barco_multi_hall.py`** - запускать только этот файл! Это единственная рабочая версия системы управления мультизалом.

## ✨ Возможности

### Управление несколькими залами
- 🎪 **Мультизал** - управление всеми залами из одного интерфейса
- 🔐 **Авторизация** - защищенный доступ с логином и паролем
- 📱 **Адаптивный дизайн** - работает на ПК, планшете и смартфоне
- 📋 **Логирование** - автоматическое сохранение всех действий администраторов
- 🔄 **Real-time обновления** - WebSocket для мгновенного отображения изменений
- 💾 **Конфигурация из файла** - легко добавлять/удалять залы через JSON

### Функции управления
- ▶️ **Play / Pause / Stop** - управление воспроизведением
- 🗑️ **Clear** - очистка плейлиста
- 💡 **Lamp ON/OFF** - управление лампой проектора
- 🔊 **Volume +/- / Mute** - управление громкостью
- 🎭 **Shutdown Session** - корректное завершение сеанса (выкл. лампы + громкость на минимум)
- 🎨 **EKOS Light Control** - команды управления освещением через EKOS

## 📦 Требования

- Python 3.6+
- Flask 2.3+
- Flask-SocketIO 5.3+

## 🚀 Быстрый старт

### 1. Установка зависимостей

**Windows:**
```bash
pip install -r requirements.txt
```

**Linux (Debian/Ubuntu) с виртуальным окружением:**

Современные версии Debian/Ubuntu блокируют установку пакетов через pip напрямую (ошибка `externally-managed-environment`). Используйте виртуальное окружение:

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать его
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

После этого для запуска всегда сначала активируйте окружение:
```bash
source venv/bin/activate
python barco_multi_hall.py
```

### 2. Настройка залов

Скопируйте `halls_config.example.json` в `halls_config.json` и укажите реальные IP-адреса:

```json
{
  "hall1": {"name": "Зал 1", "ip": "192.168.118.61", "port": 43748},
  "hall2": {"name": "Зал 2", "ip": "192.168.118.62", "port": 43748}
}
```

### 3. Настройка приветствий (опционально)

Отредактируйте файл `greetings.txt` - по одному приветствию на строку:

```
Добро пожаловать в панель управления!
С возвращением, администратор!
Приятной работы!
```

### 4. Запуск

**Обычный запуск:**
```bash
python barco_multi_hall.py
```

**Docker Compose (рекомендуется для продакшена):**

```bash
# Создать конфигурацию залов
cp halls_config.example.json halls_config.json
nano halls_config.json  # Указать реальные IP

# Запустить контейнер
docker-compose up -d

# Посмотреть логи
docker-compose logs -f

# Остановить
docker-compose down
```

Откройте браузер: http://localhost:5000

**Данные для входа по умолчанию:**
- Логин: `admin`
- Пароль: `admin`

⚠️ **Важно:** Измените пароль в файле `barco_multi_hall.py` перед использованием в продакшене!

## 📱 Интерфейс

- **Выбор зала** - выпадающий список в шапке страницы
- **Статус соединения** - индикатор подключения к проектору
- **Панель управления** - все команды воспроизведения и управления
- **Лог операций** - real-time журнал действий с временными метками
- **Кнопка выхода** - завершение сеанса и возврат к странице авторизации

## 📝 Логирование

Все действия автоматически записываются в файлы:

```
logs/admin_actions_YYYY-MM-DD.log
```

Формат записи:
```
[HH:MM:SS] Админ: Имя | Зал: hall1 | Действие: Play | Успешно
```

## 🏗️ Структура проекта

```
easycinemacontrol/
├── barco_multi_hall.py          # 🎯 Главный файл - запускать этот!
├── halls_config.json            # Конфигурация залов (создать из .example)
├── halls_config.example.json    # Пример конфигурации
├── greetings.txt                # Приветствия для страницы входа
├── requirements.txt             # Python зависимости
├── Dockerfile                   # Docker образ
├── docker-compose.yml           # Docker Compose конфигурация
├── templates/
│   ├── halls.html              # Интерфейс управления залами
│   └── login.html              # Страница авторизации
├── static/
│   ├── halls.css               # Стили (адаптивные)
│   └── halls.js                # Логика (WebSocket)
└── logs/                       # Логи действий (создается автоматически)
```

## 🐳 Docker Deployment

### Преимущества Docker
- ✅ Изолированное окружение
- ✅ Автозапуск при перезагрузке сервера
- ✅ Простое обновление версий
- ✅ Не нужно настраивать Python окружение

### Быстрый старт с Docker

```bash
# 1. Склонировать репозиторий
git clone https://github.com/pattokigoven/easycinemacontrol.git
cd easycinemacontrol

# 2. Создать конфигурацию
cp halls_config.example.json halls_config.json
nano halls_config.json  # Указать реальные IP адреса залов

# 3. Запустить
docker-compose up -d

# 4. Проверить статус
docker-compose ps
docker-compose logs -f
```

### Управление контейнером

```bash
# Остановить
docker-compose down

# Перезапустить
docker-compose restart

# Обновить до новой версии
git pull
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# Посмотреть логи
docker-compose logs -f easycinema
```

### Volumes (монтирование файлов)

Docker Compose автоматически монтирует:
- `halls_config.json` - конфигурация залов (read-only)
- `greetings.txt` - приветствия (read-only)
- `logs/` - директория с логами (read-write)

Это позволяет редактировать конфигурацию без пересборки образа.

## 🔧 Дополнительная настройка

### Изменение порта сервера

В файле `barco_multi_hall.py`:

```python
if __name__ == "__main__":
    socketio.run(app, host='0.0.0.0', port=5000, debug=True)  # Измените порт здесь
```

### Изменение учетных данных

В файле `barco_multi_hall.py` найдите словарь `USERS`:

```python
USERS = {
    "admin": "your_secure_password_here"
}
```

### Протокол Barco ICMP

Система использует официальный протокол Barco ICMP Automation over IP:
- Порт: **43748** (стандартный)
- Режим: ACK (автоматическое подтверждение команд)
- Кодировка: UTF-8

## 📚 Документация

- [DEPLOYMENT.md](DEPLOYMENT.md) - Полное руководство по развертыванию в продакшн (Linux, systemd, nginx)
- [CHECKLIST.md](CHECKLIST.md) - Чеклист для запуска в продакшене

## 🐛 Устранение неполадок

### Ошибка подключения к залу

- Проверьте IP-адрес в `halls_config.json`
- Убедитесь, что проектор включен и подключен к сети
- Проверьте, что порт 43748 не заблокирован файрволом
- Попробуйте выполнить `ping` до IP-адреса проектора

### Не работает WebSocket

- Убедитесь, что установлена Flask-SocketIO версии 5.3+
- Проверьте, что не используется proxy, блокирующий WebSocket
- В настройках nginx добавьте поддержку WebSocket (см. DEPLOYMENT.md)

### Не сохраняются логи

- Убедитесь, что папка `logs/` создана и доступна для записи
- Проверьте права доступа: `chmod 755 logs/`

## 📖 Лицензия

MIT License

## 🔗 Ссылки

- **GitHub:** https://github.com/pattokigoven/easycinemacontrol
- **Документация Barco ICMP:** Barco ICMP - Automation over IP Protocol.0.8.pdf
