# 🚀 Инструкция по загрузке на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите "+" → "New repository"
3. Заполните:
   - **Repository name**: `barco-icmp-control` (или любое другое имя)
   - **Description**: Barco ICMP Multi-Hall Control System
   - **Visibility**: Private (рекомендуется) или Public
   - ❌ **НЕ** отмечайте "Initialize with README" (у нас уже есть README)
4. Нажмите "Create repository"

## Шаг 2: Подключите удаленный репозиторий

GitHub покажет вам команды. Используйте эти:

```bash
cd c:\lighttms

# Добавьте удаленный репозиторий (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Переименуйте ветку в main (современный стандарт)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

### Пример с конкретными именами:
```bash
git remote add origin https://github.com/ivanov/barco-icmp-control.git
git branch -M main
git push -u origin main
```

## Шаг 3: Введите учетные данные

При первом пуше GitHub попросит авторизацию:

### Вариант A: Personal Access Token (рекомендуется)
1. Перейдите: Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажмите "Generate new token (classic)"
3. Выберите срок действия и права:
   - ✅ `repo` (полный доступ к репозиториям)
4. Сгенерируйте и **сохраните токен** (он больше не отобразится!)
5. При пуше вместо пароля используйте токен

### Вариант B: SSH ключ
```bash
# Генерация SSH ключа
ssh-keygen -t ed25519 -C "your_email@example.com"

# Копирование публичного ключа
# Windows
type C:\Users\YourName\.ssh\id_ed25519.pub

# Linux/Mac
cat ~/.ssh/id_ed25519.pub
```

Добавьте ключ в GitHub: Settings → SSH and GPG keys → New SSH key

Измените remote URL:
```bash
git remote set-url origin git@github.com:YOUR_USERNAME/REPO_NAME.git
git push -u origin main
```

## Шаг 4: Проверка

Обновите страницу репозитория на GitHub - вы должны увидеть все файлы!

## 📤 Последующие обновления

После внесения изменений:

```bash
# Добавить все изменения
git add .

# Или добавить конкретные файлы
git add barco_multi_hall.py

# Создать коммит
git commit -m "Описание изменений"

# Загрузить на GitHub
git push
```

## 🔄 Полезные команды

```bash
# Проверить статус
git status

# Посмотреть историю
git log --oneline

# Посмотреть изменения
git diff

# Отменить изменения в файле
git checkout -- filename

# Посмотреть удаленные репозитории
git remote -v

# Загрузить изменения с GitHub
git pull
```

## 🌿 Работа с ветками

```bash
# Создать новую ветку
git checkout -b feature-new-hall

# Переключиться на ветку
git checkout main

# Слить ветку
git merge feature-new-hall

# Удалить ветку
git branch -d feature-new-hall
```

## 📝 Пример workflow

```bash
# 1. Создали новую функцию
nano barco_multi_hall.py

# 2. Проверили изменения
git status
git diff

# 3. Добавили в коммит
git add barco_multi_hall.py

# 4. Создали коммит
git commit -m "Add automatic reconnection feature"

# 5. Загрузили на GitHub
git push

# 6. Проверили на GitHub
```

## 🚨 Важно

### НЕ коммитьте конфиденциальные данные!

Файлы уже в `.gitignore`:
- `logs/` - логи с именами администраторов
- `halls_config.json` - реальные IP адреса залов

Вместо этого коммитится `halls_config.example.json` с примером.

### Если случайно закоммитили секреты:

```bash
# Удалить файл из истории (ОСТОРОЖНО!)
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch halls_config.json" \
  --prune-empty --tag-name-filter cat -- --all

# Принудительный push
git push origin --force --all
```

## 🎯 Готово!

Теперь ваш проект на GitHub и готов к развертыванию на других устройствах!

### Клонирование на другом устройстве:

```bash
git clone https://github.com/YOUR_USERNAME/REPO_NAME.git
cd REPO_NAME
pip install -r requirements.txt
cp halls_config.example.json halls_config.json
nano halls_config.json
python barco_multi_hall.py
```

---

**Следующие шаги:**
1. ✅ Создайте репозиторий на GitHub
2. ✅ Выполните команды из Шага 2
3. ✅ Проверьте, что код загрузился
4. ✅ Попробуйте склонировать на другое устройство
